import React from 'react';

import Calendar from 'containers/Home/Calendar';

const Home: React.FC = () => (
  <main>
    <section>
      <Calendar />
    </section>
  </main>
);

export default Home;
